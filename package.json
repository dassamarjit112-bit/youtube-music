{
  "name": "youtube-music-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "sync": "cd frontend && npm run build && npx cap sync android",
    "open:android": "npx cap open android"
  }
}
