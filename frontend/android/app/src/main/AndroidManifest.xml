<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--
            MusicControlsService (from capacitor-music-controls-plugin)
            stopWithTask="false" → service stays running when user swipes app from Recents.
            This is the KEY flag that enables true background playback.
        -->
        <service
            android:name="com.capacitorjs.plugins.musiccontrols.MusicControlsService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaPlayback" />

        <!--
            Our custom MusicPlayerService:
            - stopWithTask="false" → survives task removal (swipe from Recents)
            - START_STICKY in onStartCommand → auto-restarts if OS kills it
            - Holds WakeLock + AudioFocus to prevent audio interruption
        -->
        <service
            android:name=".MusicPlayerService"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

    </application>

    <!-- ── Permissions ─────────────────────────────────────────────────────── -->

    <!-- Show media notification on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network access for streaming -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- CRITICAL: Keep CPU on during background playback -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Required to call startForegroundService() -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Required for foregroundServiceType="mediaPlayback" on API 34+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Access the microphone for search or voice features -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Control audio levels and handle interruptions -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

</manifest>
